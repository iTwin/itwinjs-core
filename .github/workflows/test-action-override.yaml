# iTwin.js Github Actions Test - if you see this in master... delete it

name: Extract API

on:
  pull_request:
    branches:
      - master
      - release/*
    paths-ignore:
      - "**.md"
      - docs/**
      - .github/workflows/test-action.yaml
      - .github/workflows/test-action-override.yaml
      - common/config/azure-pipelines/jobs/test-pipeline.yaml

jobs:
  extract-api:
    runs-on: ubuntu-latest
    steps:
      - name: Override extract-api Test
        run: |
          echo this is a test