---
publish: false
---
# NextVersion

## @itwin/core-backend and @itwin/core-frontend

- Added `@alpha` backend-to-frontend IPC support using [IpcHost]($backend), [IpcApp]($frontend), and [IpcHandler]($frontend).
- This enables request/response style calls from backend to frontend IPC channels.

