{
  "name": "@bentley/ecpresentation-frontend",
  "version": "0.1.2",
  "description": "Frontend of ECPresentation library",
  "keywords": [
    "Bentley",
    "EC",
    "Presentation",
    "iModelJS",
    "Frontend"
  ],
  "license": "UNLICENSED",
  "author": {
    "name": "Bentley Systems, Inc.",
    "url": "http://www.bentley.com"
  },
  "main": "lib/index.js",
  "typings": "lib/index",
  "scripts": {
    "build": "tsc -p ./src",
    "clean": "rimraf lib package-deps.json",
    "cover": "cross-env TS_NODE_PROJECT=./tests nyc npm test -- --coverage",
    "lint": "tslint -p ./src",
    "pack": "node ../../scripts/pack.js",
    "test": "node ../../scripts/run-tests.js --testsDir ./tests",
    "test+cover": "cross-env TS_NODE_PROJECT=./tests nyc npm test -- --coverage --report",
    "validatePackageJson": "validjson ./package.json",
    "watch": "npm test -- --watch"
  },
  "dependencies": {
    "@bentley/ecpresentation-common": "^0.1.2"
  },
  "//peerDependencies": [
    "No need to repeat all of the dependencies that I have in common with frontend."
  ],
  "peerDependencies": {
    "@bentley/imodeljs-frontend": "^0.82"
  },
  "devDependencies": {
    "@bentley/bentleyjs-core": "^8",
    "@bentley/imodeljs-common": "^0.82",
    "@bentley/imodeljs-frontend": "^0.82",
    "cross-env": "^5.1.0",
    "nyc": "^11.6.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.9.0",
    "typescript": "^2.8.3",
    "valid-json-cli": "^1.3.0"
  },
  "nyc": {
    "nycrc-path": "../../.nycrc",
    "report-dir": "../../out/reports/coverage/frontend",
    "temp-directory": "../../out/reports/coverage/frontend/.nyc_output"
  }
}