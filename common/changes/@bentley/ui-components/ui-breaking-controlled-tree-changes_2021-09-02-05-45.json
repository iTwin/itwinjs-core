{
  "changes": [
    {
      "packageName": "@bentley/ui-components",
      "comment": "Change `ControlledTree` to take `TreeModel` instead of `VisibleTreeNodes`.",
      "type": "none"
    },
    {
      "packageName": "@bentley/ui-components",
      "comment": "Replace `useVisibleTreeNodes` with `useTreeModel` since consumers don't need to create `VisibleTreeNodes` anymore.",
      "type": "none"
    }
  ],
  "packageName": "@bentley/ui-components"
}