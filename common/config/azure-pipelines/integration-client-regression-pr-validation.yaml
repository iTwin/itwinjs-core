# iModel.js Integration Client Regression PR Validation Build
#
# This integration test job uses the older internal OIDC client and IMS authority to ensure
# that workflow is still supported.

trigger:
  - master

variables:
  - group: iModel.js non-secret internal config variables
  - group: iModel.js Integration Test Users

jobs:
  - job: Node_14_x
    condition: succeeded()
    pool:
      vmImage: ubuntu-latest
    steps:
      - template: templates/integration-test-steps.yaml
        parameters:
          Node_Version: 14.x
