# iModel.js CI Build

trigger:
  - master

jobs:
  - template: ci-core.yaml
    parameters:
      name: Node_12
      nodeVersion: 12.17.0
      pool:
        demands:
        - Agent.OS -equals $(platform)

