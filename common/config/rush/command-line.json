{
  "$schema": "https://dev.office.com/json-schemas/rush/command-line.schema.json",
  "customCommands": [
    {
      "name": "clean",
      "summary": "Cleans build output within each package",
      "documentation": "Iterates through each package in the monorepo and runs the 'clean' script",
      "parallelized": true
    },
    {
      "name": "docs",
      "summary": "Builds the API reference docs for each package",
      "documentation": "Iterates through each package in the monorepo and runs the 'docs' script",
      "parallelized": true
    },
    {
      "name": "lint",
      "summary": "Run lint rules on each package",
      "documentation": "Iterates through each package in the monorepo and runs the 'lint' script",
      "parallelized": true
    }
  ]
}