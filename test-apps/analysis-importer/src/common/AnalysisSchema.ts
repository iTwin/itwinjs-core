/*---------------------------------------------------------------------------------------------
|  $Copyright: (c) 2018 Bentley Systems, Incorporated. All rights reserved. $
*--------------------------------------------------------------------------------------------*/
// __PUBLISH_EXTRACT_START__ Schema.schemaNames

/** Constants associated with Analysis schema. */
/** The "class full names" (SchemaName:ClassName) for the classes associated with Analysis. */
export enum Class {
  Mesh = "Analysis:Mesh",
}

/** The CodeSpec names associated with Analysis. */
export enum CodeSpec {
  Node = "Analysis:Mesh",
}

/** The SpatialCategory names associated with Analysis. */
export enum Category {
  Node = "Analysis:Mesh",
}

// __PUBLISH_EXTRACT_END__
