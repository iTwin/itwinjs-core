import * as __React from "react";
import * as __chai from "chai";
import * as __sinon from "sinon";
import * as __enzyme from "enzyme";

declare global {
  const React: typeof __React;
  const sinon: typeof __sinon;
  const expect: typeof __chai.expect;
  const shallow: typeof __enzyme.shallow;
  const mount: typeof __enzyme.mount;
}